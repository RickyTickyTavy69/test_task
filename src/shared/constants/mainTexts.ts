const MainTexts = {
    modalLoadJSON : `here you can load your own json data. 
    It should be the right format to be loaded correctly (just files like sequence_assessment.json or the app will crash :/ )`,
    upLoad: "upLoad File",
} as const;

export default MainTexts;